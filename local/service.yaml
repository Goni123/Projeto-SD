apiVersion: v1
kind: Service
metadata:
  name: projeto
  labels:
    app: projeto
spec:
  type: NodePort
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
    nodePort: 30001
  selector:
    app: projeto